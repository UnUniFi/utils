version: "3"

services:
  app:
    build: .
    # ports:
    #   - 1317:1317
    #   - 9090:9090
    #   - 26656:26656
    #   - 26657:26657
    volumes:
      - ./band:/root/.band
    command: /bin/ash
    tty: true
    # network_mode: host
    # restart: always