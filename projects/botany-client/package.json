{
  "name": "botany-client",
  "version": "0.3.6",
  "description": "REST API Client for JPYX and EURX (Cosmos SDK) Blockchain",
  "main": "index.js",
  "scripts": {
    "lint": "eslint \"src/**/*\"",
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "tsc --build --clean && tsc && node bundle",
    "gen:openapi": "bash ./gen-openapi.sh",
    "gen:proto": "bash ./gen-proto.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cosmos-client": "^0.42.2-3",
    "protobufjs": "^6.10.2"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/jest": "^26.0.20",
    "@types/long": "^4.0.1",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.3",
    "typescript": "^4.2.3"
  }
}